# DetectorTecho (/docs/personaje/v1/referencia/detector-techo)



`DetectorTecho` se utiliza junto a `Jugador` para registrar colisiones que representan un techo y permitir que el controlador reaccione durante el salto.

Se inicializa con la transformación del jugador y se expone mediante `ObtenerDetector()`.
